{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^5.0.0",
    "three": "^0.154.0"
  },
  "devDependencies": {
    "vite": "^4.3.9"
  }
}
